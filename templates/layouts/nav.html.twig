<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
	<div class="container">
		<a class="navbar-brand" href="{{ path('home') }}">{{ 'navbar.brand'|trans }}</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarScroll">
			<ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="{{ path('home') }}">{{ 'navbar.home'|trans }}</a>
				</li>

				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							{{ 'navbar.moderation'|trans }}
						</a>
						<ul class="dropdown-menu">
							{% if is_granted('ROLE_SUPER_ADMIN') %}
								<li class="nav-item">
									<a href="{{ path('superadmin_unpurchased_carts') }}" class="nav-link">{{ 'navbar.unpurchased_carts'|trans }}</a>
								</li>
								<li class="nav-item">
									<a href="{{ path('superadmin_recent_users') }}" class="nav-link">{{ 'navbar.recent_users'|trans }}</a>
								</li>
								<li><hr class="dropdown-divider"></li>
							{% endif %}
							<li>
								<a class="nav-link" href="{{ path('products') }}">{{ 'navbar.products'|trans }}</a>
							</li>
							<li>
								<a class="nav-link" href="{{ path('product_new') }}">{{ 'navbar.new_products'|trans }}</a>
							</li>
						</ul>
					</li>
				{% endif %}
			</ul>
			<ul class="navbar-nav ms-auto">
				{% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('cart_show') }}">{{ "navbar.cart"|trans }}</a>
                    </li>
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							{{ app.user.firstName }}
							{{ app.user.lastName|upper }}
						</a>
						<ul class="dropdown-menu">
							<li class="nav-item">
								<a class="nav-link" href="{{ path('account') }}">{{ "navbar.profile"|trans }}</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{ path('app_logout') }}">{{ 'navbar.logout'|trans }}</a>
							</li>
						</ul>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_register') }}">{{ 'navbar.register'|trans }}</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_login') }}">{{ 'navbar.login'|trans }}</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
